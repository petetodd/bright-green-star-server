# Using MYSQL 
development:
    adapter: mysql2
    encoding: utf8
    reconnect: false
    database: bgs_demo_dev
    pool: 5
    username: demobgs
    password: demobgs
    socket: /tmp/mysql.sock 

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    adapter: mysql2
    encoding: utf8
    reconnect: false
    database: tester
    pool: 5
    username: demobgs
    password: demobgs
    socket: /tmp/mysql.sock  


production:
    adapter: mysql2
    encoding: utf8
    reconnect: false
    database: bgs_demo_prod
    pool: 5
    username: ausername
    password: acrazypassword
    socket: /var/lib/mysql/mysql.sock
